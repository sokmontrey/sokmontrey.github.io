---
interface Props {
	height?: string;
	id: string;
};

const { height, id } = Astro.props;
---

<section id={id} 
	class:list={[
		" w-full flex flex-col justify-center items-center",
		height ?? "h-screen",
	]}>
	<slot />
</section>
